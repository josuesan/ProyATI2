<%= form_for(comment) do |f| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="formulario">
    <div class="form-group col-sm-12 col-md-12 col-lg-12">
      <%= f.label :nombre, "Nombre de usuario" %>
      <%= f.text_field :nombre, class:"form-control field", :value => current_user.username, :readonly => true%>
    </div>

    <div class="form-group col-sm-12 col-md-12 col-lg-12">
      <%= f.label :descripcion, "Descripción del comentario " %>
      <%= f.text_area :descripcion, class:"form-control field" %>
    </div>

    <%= f.hidden_field :user_id, class:"form-control field", :value => current_user.id, :readonly => true  %>

    <div class="col-sm-12 col-md-12 col-lg-12">
      <%= f.submit 'Guardar', class:"btn btn-primary" %>
      <%= link_to 'Regresar', comments_path, class:"btn btn-primary" %>
    </div>
  </div>
<% end %>
